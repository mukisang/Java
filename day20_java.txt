art shift r : 찾아 바꾸기 enter 누르기
-------------------------
upcasting, down casting

상속과 연관?

upcasting
부모 클래스 타입으로 자식 생성자를 호출하는 것
부모와 자식간의 공통요소(재정의 메서드)만 사용 가능하며, 자식 클래스의 추가된 것들은 사용할 수 없다.
부모 타입으로 선언 되었기 때문에 부모의 필드만 사용 가능 하지만 자식 클래스에서 재정의 했다면, 재정의 된 메서드로 사용된다

downcasting(비추천)
자식 클래스 타입으로 부모 생성자를 호출 하는 오류
부모의 범위가 더 크기 때문에 자식에 담을 수 없다.

부모가 자식을 제어할 수 있지만
자식이 부모를 제어할 수 없다.

업 캐스팅
부모타입 변수 이름=new 자식 생성자();
	공통 메서드

자식껄로 생겨서 부모의 전부가 사용가능하고 부모 메서드가 되지만 부모에 없는 자식 메서드는 사용 불가
오버라이딩이 있는 메서드를 사용할 경우 자식의 메서드가 나옴
부모 필드에 있는건 전부 사용가능하지만 자식필드에 있는건 사용이 불가하다.

넓게 생성되서 부모에 딱 맞춰주는 거라 생각하면 편함

다운 캐스팅
자식타입 변수이름 = new 부모생성자();
불가하다. classCastException발생 자식껄 부모에 담을 수가 없음.


사용 이유
instanceof 
각자의 타입이 맞는지 확인하는 방법

값 instanceof 클래스 타입: 값이 클래스 탑이니?(조건식임)

"안녕"instanceof String 참
10 instanceof Stiring 거짓

upcasting 사용시 자식과 부모타입 전부를 갖고 있다고 생각해야한다.
-----------------------
다형성 Polymorphism

다형성이란
이름은 같으나 형태가 다른것  overloading, overriding
하나의 메소드가 서로 다른 클래스에서 다양하게 실행되는 것을 말한다.
다형성을 구현하기 위해서는 다형성을 구현할 메소드를 포함할 클래스에게
부모클래스가 상속해줘야 한다.
부모 클래스와 자식 클래스에 같은 이름의 메소드가 있어야 하고, 
자식 클래스에서는 재정의(Overriding)해야한다.