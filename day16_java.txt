RIot r= new Riot
r은 객체 인스턴스라고 말함

절대 경로 사진 옆에 propertys??들어가서 주소로 이용하는 것

split("")
JOptionPane,showInputDialog("").split(",");
문자열에 구분점을 주면 그 구분점의 기준으로 나눠서 배열로 전달

control+메소드 클릭->해당 메서드가 있는 곳으로 이동됨

윈도우 빌더
--------------------------------------------------------------

